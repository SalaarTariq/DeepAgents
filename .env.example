# Copy to `.env` and fill in. Only the keys for the providers/tools you actually use are required.
OPENAI_API_KEY=
GROQ_API_KEY=
TAVILY_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: override the default model used by examples.
# Format is LangChain's `provider:model`, e.g. `openai:gpt-5` or `anthropic:claude-sonnet-4-5-20250929`.
# DEEP_AGENT_MODEL=groq:qwen/qwen3-32b
